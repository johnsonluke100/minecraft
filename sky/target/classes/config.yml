# SkyLighting — 8XD continuum config

hypercube:
  # 14-float hypercube axes (names only, values come from runtime math)
  axes:
    - a # east-lean / Jerusalem / unfolding origin
    - b # north-lean
    - c # west-lean
    - d # south-lean
    - e # polar-north
    - f # polar-south
    - g # expansion
    - z
    - y
    - x
    - w
    - v
    - u
    - t

reach:
  max_blocks: 8

mic:
  json_path: bpm_sync.json
